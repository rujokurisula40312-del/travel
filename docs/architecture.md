# Техническая архитектура

> Этот документ закрывает то, чего нет в [`product-vision.md`](product-vision.md): как именно бот устроен внутри, как разнесены данные, как изолированы ПД от Claude, как работает мультиклиентность.

---

## 1. Карта компонентов

```
                        ┌────────────────────────────┐
                        │       Telegram Bot API     │
                        └─────────────┬──────────────┘
                                      │ long polling
                                      ▼
┌──────────────────────────────────────────────────────────────┐
│                    Travel Bot (Python, aiogram)               │
│                                                               │
│  ┌────────────┐   ┌──────────────┐   ┌───────────────────┐    │
│  │  Routers   │──▶│  FSM /       │──▶│  Use-case layer   │    │
│  │ (handlers) │   │  Dialogue    │   │ (booking, lookup, │    │
│  └────────────┘   │  state       │   │  ai_consult, ...) │    │
│        │          └──────────────┘   └─────────┬─────────┘    │
│        │                                        │              │
│        ▼                                        ▼              │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                    Service layer                        │   │
│  │  ┌──────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐  │   │
│  │  │ Sheets   │  │  Drive   │  │ Notion │  │  Claude  │  │   │
│  │  │ service  │  │  service │  │ service│  │  service │  │   │
│  │  └────┬─────┘  └────┬─────┘  └───┬────┘  └────┬─────┘  │   │
│  └───────┼─────────────┼────────────┼────────────┼────────┘   │
│          │             │            │            │            │
│          │   ┌─────────┴────────────┘            │            │
│          │   │  PII boundary  ───────────────────│────────────┤
│          │   │  (только обезличенный контекст уходит вправо) │
└──────────┼───┼───────────────────────────────────┼────────────┘
           ▼   ▼                                    ▼
   ┌──────────────────┐  ┌──────────────┐   ┌─────────────────┐
   │   Google Sheets  │  │ Google Drive │   │  Notion + Claude │
   │  (RU-данные ТА,  │  │   (PDF туров)│   │  (контент, FAQ,  │
   │   ПД туристов)   │  │              │   │   ИИ-ответы)     │
   └──────────────────┘  └──────────────┘   └─────────────────┘
       РФ-зона                  РФ-зона          Зарубежная зона
       (ПД OK)                  (без ПД)         (БЕЗ ПД!)
```

### Важно: PII-граница

ПД туристов **никогда** не уходят в Claude и Notion. Всё, что отправляется в Claude — обезличенный контекст:

> Турист интересуется Сейшелами на двоих в апреле, бюджет ~200к, спрашивает про визу.

Имя, телефон, email — только в Google Sheets, который шарится с почтой ТА в России.

---

## 2. Структура проекта (Python-модули)

```
travel_bot/
├── bot.py                     # точка входа, запускает Dispatcher
├── config/
│   ├── __init__.py
│   ├── settings.py            # глобальные переменные окружения (логи, токены)
│   └── client_config.py       # модель конфига одного клиента (см. data-schemas.md)
│
├── routers/
│   ├── start.py               # /start, главное меню
│   ├── menu.py                # обработка кнопок (программа, чек-лист, оплата)
│   ├── ai_chat.py             # свободный текст → ИИ-консультант
│   ├── booking.py             # сценарий записи (FSM)
│   ├── handover.py            # перевод на менеджера
│   └── consent.py             # согласие ФЗ-152, /delete_me
│
├── services/
│   ├── sheets.py              # gspread: туры, лиды, лист ожидания, конфиг
│   ├── drive.py               # выдача PDF по country_id
│   ├── notion.py              # загрузка контента страны
│   ├── claude.py              # клиент Anthropic SDK + сборка промпта
│   └── notifier.py            # отправка уведомлений менеджерам
│
├── usecases/
│   ├── find_tour.py           # «есть ли тур в страну X»
│   ├── send_program.py        # PDF по стране/туру
│   ├── ai_consult.py          # вопрос → ответ ИИ + контекст из Notion
│   ├── book_seat.py           # append лида в лист тура (с флагом is_waitlist)
│   └── delete_me.py           # удаление ПД по запросу
│
├── domain/
│   ├── models.py              # dataclass: Tour, Lead, Country, ConsentRecord
│   ├── states.py              # FSM-состояния (см. conversation-flow.md)
│   └── errors.py              # доменные ошибки (CountryNotFound, ...)
│
├── infra/
│   ├── google_auth.py         # service account
│   ├── cache.py               # TTL-кеш для туров и Notion
│   └── logging.py
│
└── prompts/
    ├── system_prompt.j2       # шаблон системного промпта (Jinja2)
    └── moderation.j2          # промпт для отсева оффтопа
```

> **Принцип:** routers — тонкие, бизнес-логика — в `usecases`, внешние API — в `services`, доменные модели — в `domain`. Так проще писать тесты и переиспользовать use-case в разных хендлерах.

---

## 3. Ключевые потоки данных

### 3.1 Запрос «дай программу по Китаю»

```
User                Router          Usecase            Sheets           Drive
 │     /menu →       │                │                  │                │
 │ "программа Китай" │                │                  │                │
 │ ──────────────────▶                │                  │                │
 │                   │ send_program(  │                  │                │
 │                   │   country=CN)──▶                  │                │
 │                   │                │ get_country(CN)──▶                │
 │                   │                │  ◀── pdf_file_id │                │
 │                   │                │ get_pdf(file_id) ────────────────▶│
 │                   │                │  ◀────────────────── PDF bytes    │
 │                   │  ◀── PDF       │                  │                │
 │  ◀── document     │                │                  │                │
```

**Гарантия:** имя файла **не** используется как ключ. В Sheets есть колонка `pdf_file_id` (Google Drive ID). Это убирает проблему транслитерации, нескольких файлов на страну, опечаток.

### 3.2 ИИ-вопрос «нужна ли виза в Сейшелы»

```
User              ai_chat router      ai_consult uc       Notion        Claude
 │ "виза в Сейшелы?" ──▶              │                    │              │
 │                   │  ai_consult(   │                    │              │
 │                   │  text, ctx) ───▶                    │              │
 │                   │                │ get_country_kb(SC)─▶              │
 │                   │                │  ◀── markdown      │              │
 │                   │                │ build_prompt(...)  │              │
 │                   │                │ messages.create() ─────────────────▶
 │                   │                │  ◀───────────────── reply         │
 │                   │  ◀── reply     │                    │              │
 │  ◀── ответ        │                │                    │              │
```

**Что уходит в Claude:**
- системный промпт (роль, тон, ограничения, имя ТА)
- контекст страны из Notion (markdown)
- последние N реплик диалога **без** ФИО/телефона/email
- свежий вопрос пользователя

**Что НЕ уходит:**
- никакие данные из листа «Туристы»
- никаких telegram_id, имён, контактов

### 3.3 Сценарий записи на тур

См. [`conversation-flow.md`](conversation-flow.md) — подробный FSM. Здесь — ключевая часть:

```
                      ┌────────────────────────────────┐
                      │  Пользователь подтвердил тур   │
                      └─────────────┬──────────────────┘
                                    ▼
                      ┌────────────────────────────────┐
                      │  Запрос согласия (ФЗ-152)      │
                      │  Inline-кнопка "Согласен"      │
                      └─────────────┬──────────────────┘
                                    ▼
                      ┌────────────────────────────────┐
                      │  Сбор: ФИО → телефон → email   │
                      └─────────────┬──────────────────┘
                                    ▼
                      ┌────────────────────────────────┐
                      │  sheets.append_lead(            │
                      │     tour_sheet, lead)           │
                      │  is_waitlist = seats < pax      │
                      └─────────────┬──────────────────┘
                                    ▼
                      ┌────────────────────────────────┐
                      │  Уведомить менеджера(ов)       │
                      │  Подтвердить туристу           │
                      └────────────────────────────────┘
```

**Про места.** Бот не занимает места — реальная бронь происходит после предоплаты, которую принимает менеджер вживую. Это сильно упрощает архитектуру:

- Никаких блокировок, локов, атомарных декрементов.
- `available_seats` в Sheets — справочное поле для информирования туриста («осталось 3 места»). Обновляет менеджер вручную после оплат.
- Если `available_seats == 0` — лид всё равно записывается, но с `is_waitlist=TRUE`. Бот честно говорит: «формально мест уже нет, но если кто-то откажется — менеджер свяжется с вами».
- Двое одновременно могут записаться на одно место — оба получают «менеджер свяжется», конфликта нет.

---

## 4. Управление состоянием диалога (FSM)

aiogram 3.x даёт `StatesGroup`. Используем `MemoryStorage` для MVP, `RedisStorage` для прода (чтобы переживать рестарт).

Ключевые группы состояний (детали в [`conversation-flow.md`](conversation-flow.md)):

```python
class Booking(StatesGroup):
    waiting_country = State()
    waiting_dates = State()
    waiting_pax = State()
    waiting_budget = State()
    waiting_consent = State()
    waiting_name = State()
    waiting_phone = State()
    waiting_email = State()
    confirming = State()
```

> Отдельной FSM `Waitlist` нет — лист ожидания это просто флаг `is_waitlist=TRUE` в строке лида. Сценарий записи одинаковый и для свободных мест, и для переполненного тура, отличается только последняя реплика бота.

Контекст диалога (`state.set_data`) хранит:
- `lead_draft` — заполняемые поля лида
- `chat_history` — последние 6 реплик для подачи в Claude (без ПД)
- `current_tour_sheet` — в какой лист писать
- `consent_at` — timestamp согласия

---

## 5. Мультиклиентность (SaaS-режим)

### 5.1 Один процесс vs процесс-на-клиента

| Подход | Когда |
|---|---|
| **Процесс-на-клиента** (отдельный systemd-сервис на каждого ТА) | До 5–10 клиентов. Простая изоляция, легко обновлять одного. |
| **Один процесс, N токенов** (`Bot` инстансы в `Dispatcher`) | От 10 клиентов. Экономит память и деплой. |

> **Рекомендация для старта:** процесс-на-клиента на Railway/Selectel, через `docker-compose` с шаблоном сервиса. Когда клиентов станет 10+ — переход на multi-bot dispatcher.

### 5.2 Конфиг клиента

Один Google Sheets-документ на клиента (ТА). Внутри — служебные листы с префиксом `_` и **один лист на каждый тур**:

```
[Клиент: Светлана Тур]
├── _config                  # настройки бота: токен, ID Drive, менеджеры, тон ИИ
├── _audit                   # служебный журнал (consent, delete_me, errors)
├── Китай апрель 2025        # ← лист тура
│       (метаданные сверху + список туристов снизу)
├── Сейшелы май 2025         # ← лист тура
├── ЮАР сентябрь 2025        # ← лист тура
└── ...
```

**Соглашение об именовании листов:**
- Листы с префиксом `_` — служебные, бот не считает их турами.
- Все остальные листы = активные туры.
- Имя листа клиент придумывает свободно (например, `Китай апрель 2025`). Это же имя бот показывает туристу как название тура.

**Жизненный цикл конфига:**
1. Ты создаёшь шаблон таблицы (`_config`, `_audit`, шаблонная вкладка тура) и шаришь клиенту с правами «редактор».
2. Клиент вписывает в `_config` токен от `@BotFather`, telegram_id менеджеров, ID Drive-папки.
3. Клиент дублирует шаблонную вкладку тура (`Шаблон_тура` → `Китай апрель 2025`), заполняет метаданные сверху.
4. Ты регистрируешь `client_id → spreadsheet_id` в **мастер-таблице** (одна строка на клиента).
5. Бот при старте читает мастер-таблицу → для каждого активного клиента читает его `_config` → запускает инстанс.
6. Изменения в `_config` и в листах туров подтягиваются по TTL (10 мин) или по команде `/reload`.

**Мастер-таблица** — один Google Sheets, где у тебя строки клиентов: `client_id`, `spreadsheet_id`, `enabled`, `notes`. Это твой реестр SaaS.

См. полные схемы листов в [`data-schemas.md`](data-schemas.md).

> ⚠️ **Безопасность токена.** Токен бота даёт полный контроль над диалогами. Раз он лежит в `_config`, доступ к таблице = доступ к боту. Клиент должен понимать, что **никому нельзя давать редактор-доступ к этой таблице, кроме доверенных сотрудников ТА**. При компрометации — `/revoke` в `@BotFather` → новый токен → правка ячейки → `/reload`.

### 5.3 Параметризация промпта

Системный промпт — Jinja2-шаблон. Подставляются: имя агентства, имя бота, тон («дружеский/официальный»), список активных направлений, контакт менеджера.

```jinja
Ты — ИИ-консультант турагентства "{{ agency_name }}".
Тон общения: {{ tone }}.
Ты можешь отвечать на вопросы о следующих направлениях: {{ countries|join(', ') }}.
...
```

---

## 6. Кеширование и синхронизация данных

| Данные | Где живут | Кеш | TTL |
|---|---|---|---|
| Список листов-туров | Sheets API | in-memory | 5 минут |
| Метаданные конкретного тура | первые строки листа тура | in-memory | 5 минут |
| Контент страны | Notion | in-memory | 15 минут |
| PDF | Google Drive | in-memory (bytes) | 1 час |
| Конфиг клиента (`_config`) | Google Sheets | in-memory | 10 минут |

> Для MVP — простой `dict + asyncio.Event` с TTL. Для прода — `aiocache` или Redis.

**Инвалидация:** команда `/reload` для админа (telegram_id из конфига) сбрасывает все кеши вручную. Этого хватает для ТА, который обновляет туры раз в день.

---

## 7. Уведомления менеджеру

`notifier.py` — отдельный сервис, чтобы можно было заменить канал (Telegram → email → CRM).

```python
async def notify_new_lead(lead: Lead, tour: Tour):
    text = render_template("new_lead.j2", lead=lead, tour=tour)
    for manager_id in config.manager_ids:
        await bot.send_message(manager_id, text, parse_mode="HTML")
```

Шаблон сообщения:

```
🟢 Новая запись на тур

Тур: <b>{{ tour.name }}</b>
Даты: {{ tour.dates }}
Турист: {{ lead.full_name }}
Телефон: <code>{{ lead.phone }}</code>
Email: {{ lead.email }}
Запрос: {{ lead.notes }}

Согласие: {{ lead.consent_at }}
```

---

## 8. Перевод на менеджера

Два варианта реализации:

| Вариант | Как работает | Плюс | Минус |
|---|---|---|---|
| **«Эстафета»** | Бот пишет менеджеру «турист X хочет связаться, telegram_id = ...», менеджер сам открывает чат с туристом. | Просто, не нужен прокси-чат. | Турист должен ещё раз начать переписку. |
| **«Прокси»** | Бот переходит в `Handover.in_chat_with_manager`, форвардит сообщения туриста менеджеру и обратно. | Бесшовно для туриста. | Сложнее, нужно следить за состоянием, риск утечки. |

> Для MVP — **«эстафета»**. Прокси — в бэклог.

---

## 9. Хостинг и деплой

### MVP (фазы 1–3)

- Railway, Docker, polling.
- Секреты — Railway env vars.
- Логи — stdout → Railway dashboard.
- БД пока нет (только Sheets + in-memory).

### Прод (фаза 5)

- Selectel или Yandex Cloud (по ФЗ-152).
- Один Docker-контейнер на клиента под `systemd` или `docker-compose`.
- Sentry (сервер в РФ) для ошибок.
- Telegram-алерты в личку владельца при критических ошибках (`notifier.notify_owner`).
- Бэкап всех листов туров (где лежат лиды) раз в день — в отдельный Sheets-документ.

---

## 10. Безопасность и ФЗ-152 (технические аспекты)

| Требование | Реализация |
|---|---|
| Согласие до сбора | FSM не пускает в `waiting_name` без `state.consent_at` |
| Локализация | Sheets шарится только на email в РФ; хостинг РФ; сервис-аккаунт Google — отдельный для прода |
| Минимизация ПД | Только ФИО, телефон, email. Паспорт **не** собираем (передаём менеджеру вживую). |
| Удаление | `/delete_me` → проходит по всем листам туров → удаляет строки с этим `telegram_id` → пишет факт в `_audit` |
| Изоляция от Claude | `claude.py` принимает только `AnonymousContext` (dataclass без ПД). Тип-чек на этапе компиляции. |
| Логи без ПД | Логгер маскирует `phone`, `email`, `full_name` через `logging.Filter` |

```python
@dataclass(frozen=True)
class AnonymousContext:
    """Всё, что МОЖНО отправить в Claude. ПД сюда не кладутся by design."""
    country_kb: str
    chat_history: list[ChatTurn]   # text only, no user identifiers
    tour_summary: str | None
    user_question: str
```

---

## 11. Тестирование

| Слой | Тесты |
|---|---|
| `services/*` | Моки внешних API (gspread, notion, anthropic) — юнит. |
| `usecases/*` | Юнит на бизнес-логику, особенно `book_seat` (гонки, овербукинг, валидация). |
| `routers/*` | aiogram test fixtures + `MemoryStorage`. |
| `prompts/*` | Регрессионные на 20+ реальных вопросов с золотыми ответами. |
| E2E | Один тестовый Telegram-бот на staging, ручной чек-лист 15 сценариев. |

---

## 12. Что вынесено в `open-questions.md`

- Стратегия предотвращения овербукинга
- Notion vs встроенный markdown в Sheets
- Перевод на менеджера: «эстафета» или прокси
- Один Sheets на клиента или один общий?
- Webhook vs polling на проде
- Авторассылка из листа ожидания при освобождении места
- Notion как RAG (embeddings) или прямой fetch по country_id?

См. полный список в [`open-questions.md`](open-questions.md).
